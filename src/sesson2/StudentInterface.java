package sesson2;

public interface StudentInterface { // trong interface sẽ chỉ gồm các phương thức trừu tượng
    void running();
    int getAge();
    // cú pháp để import interface là implements , import thêm interface khác cách nhau bằng dấu phẩy
    // khác với kế thừa là có thể dùng nhiều interface trong 1 class chính
}

